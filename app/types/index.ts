export interface Note {
        id: string;
        content: string;
        createdAt: Date;
      }

  export    interface Note {
        id: string;
        name: string;
        content: string;
        lastSaved: string;
      }